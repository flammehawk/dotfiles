- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- shell:
    - sudo apt update && sudo apt install zsh

- create:
    - ~/projects
    - ~/downloads
   

- link:
    ~/.ssh:        
    ~/.zshrc:
        force: true
- shell:
   -  chsh -s $(which zsh)

